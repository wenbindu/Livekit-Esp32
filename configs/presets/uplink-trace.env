# Diagnostic preset: export only processed uplink audio over a local WebSocket
# receiver while preserving the normal chat path.

ENABLE_DEBUG_UPLINK_WS=1
ENABLE_DEBUG_DOWNLINK_WS=0
ENABLE_DEBUG_UPLINK_WAV=0
LOCAL_AUDIO_UPLINK_ONLY=0
