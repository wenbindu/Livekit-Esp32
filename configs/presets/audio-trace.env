# Diagnostic preset: export both processed uplink audio and rendered downlink
# audio over local WebSocket receivers.

ENABLE_DEBUG_UPLINK_WS=1
ENABLE_DEBUG_DOWNLINK_WS=1
ENABLE_DEBUG_UPLINK_WAV=0
LOCAL_AUDIO_UPLINK_ONLY=0
